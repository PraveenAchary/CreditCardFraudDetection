# CreditCardFraudDetection
Credit Card Fraud Detection system built with Django REST Framework and React. Trained on an imbalanced dataset using SMOTE and optimized XGBoost with Optuna to maximize fraud recall. Exposes a REST API for real-time predictions, achieving strong ROC-AUC and threshold-based deployment tuning for practical fraud detection.
